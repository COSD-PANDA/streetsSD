deployment:
  staging:
    branch: master
    commands:
      - gulp publish
      - surge --project ./site --domain sdif-stage.surge.sh
  production:
    branch: production-two
    commands:
      - gulp publish
      - gulp deploy
