general: 
  branches: 
    only:
      - master
      - production
    ignore:
      - gh-pages
deployment:
  staging:
    branch: master
    commands:
      - gulp publish
      - surge --project ./site --domain streetssd-stage.surge.sh
  production:
    branch: production
    commands:
      - git config --global user.email "max@maksimize.com"
      - git config --global user.name "Maksim Pecherskiy"
      - gulp publish
      - gulp deploy
